<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"></style>

    <style name="AppTheme.NoActionBar.Web">
        <item name="colorAccent">@color/primaryDark</item>
        <item name="android:textColorPrimary">@color/icon</item>
        <item name="android:textColorSecondary">@color/icon</item>
    </style>

    <style name="Text.Title.DetailExpanded" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">25sp</item>
        <item name="android:maxLines">2</item>
    </style>
</resources>
